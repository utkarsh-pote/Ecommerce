CREATE TABLE ordered_items_2147 (
    ID NUMBER PRIMARY KEY NOT NULL,
    ORDER_ID NUMBER,
    PRICE FLOAT NOT NULL,
    PRODUCT_ID NUMBER,
    QUANTITY NUMBER NOT NULL,
    USER_ID NUMBER,
    CONSTRAINT fk_order_ordered_items FOREIGN KEY (ORDER_ID) REFERENCES order_2147(ORDER_ID),
    CONSTRAINT fk_product_ordered_items FOREIGN KEY (PRODUCT_ID) REFERENCES Products_2147(PRODUCT_ID),
    CONSTRAINT fk_user_ordered_items FOREIGN KEY (USER_ID) REFERENCES user_2147(ID)
);

/
INSERT ALL
    INTO ordered_items_2147 (ID, ORDER_ID, PRICE, PRODUCT_ID, QUANTITY, USER_ID)
    VALUES (1, 1, 1249.9, 1, 2, 1)
    INTO ordered_items_2147 (ID, ORDER_ID, PRICE, PRODUCT_ID, QUANTITY, USER_ID)
    VALUES (2, 2, 846.0, 2, 1, 2)
SELECT * FROM dual;

/
commit