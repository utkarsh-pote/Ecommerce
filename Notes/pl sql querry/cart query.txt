CREATE TABLE cart_2147 (
    CART_ID NUMBER PRIMARY KEY NOT NULL,
    DISCOUNT FLOAT NOT NULL,
    PRICE FLOAT NOT NULL,
    PRODUCT_ID NUMBER,
    QUANTITY NUMBER NOT NULL,
    STATUS VARCHAR2(50),
    USER_ID NUMBER,
    CONSTRAINT fk_user_cart FOREIGN KEY (USER_ID) REFERENCES user_2147(ID),
    CONSTRAINT fk_product_cart FOREIGN KEY (PRODUCT_ID) REFERENCES Products_2147(PRODUCT_ID)
);
/
INSERT ALL
    INTO cart_2147 (CART_ID, DISCOUNT, PRICE, PRODUCT_ID, QUANTITY, STATUS, USER_ID)
    VALUES (1, 10.0, 1249.9, 1, 2, 'available', 1)
    INTO cart_2147 (CART_ID, DISCOUNT, PRICE, PRODUCT_ID, QUANTITY, STATUS, USER_ID)
    VALUES (2, 5.0, 846.0, 2, 1, 'available', 2)
SELECT * FROM dual;
/
commit